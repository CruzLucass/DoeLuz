@model BeneficiarioListViewModel

@foreach (var b in Model.Beneficiarios)
{
    <div>
        <a asp-action="Details" asp-route-id="@b.BeneficiarioID"><h4>@b.Nome</h4></a>
        <h3>@b.Email</h3>
        <h3>@b.Status</h3>
        <h4>@b.Endereco</h4>
        <h4>@b.Historia</h4>
        <h4>@b.Preferencia</h4>
        <h4>@b.Doacoes</h4>
    </div>
}